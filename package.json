{
  "name": "mern",
  "module": "index.ts",
  "type": "module",
  "devDependencies": {
    "@types/bun": "latest"
  },
  "scripts": {
    "sub-init": "git submodule update --init --recursive",
    "fetch": "git submodule foreach git fetch"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}